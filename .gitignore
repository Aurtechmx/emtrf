# generated / regenerable outputs (regenerated by run_all.sh; not committed)
js-study/results/
oracles/results/
figures/*.pdf
figures/*.png
figures/_png/
# real-data-audit regenerates most of results/ on each run, but the frozen
# reproduction transcript is committed and checksummed, so results/ is tracked
# here except for the timestamped run_env.json (excluded from SHA256SUMS too).
real-data-audit/results/run_env.json
# python / latex build
__pycache__/
*.pyc
*.aux
*.log
*.out
*.synctex.gz
*.fls
*.fdb_latexmk
.DS_Store
